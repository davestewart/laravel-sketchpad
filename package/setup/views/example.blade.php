<h3>Hello {{ $name }}!</h3>
<p>This is an example controller and view, just to get you started:</p>
<ul>
	<li>the controller is located at <code>{{ $controllers }}</code></li>
	<li>the view is loaded from <code>{{ $views }}</code> using <code>view('sketchpad::example')</code></li>
</ul>
<p>You can edit, rename, replace or delete this controller and/or methods as you see fit.</p>
