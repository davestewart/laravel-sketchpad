<?php namespace %namespace%;

use Illuminate\Routing\Controller as BaseController;
use davestewart\sketchpad\config\SketchpadConfig;

/**
 * Example Controller
 */
class ExampleController extends BaseController
{

    /**
     * An example controller, just to get you started
     */
    public function startHere(SketchpadConfig $config, $name = 'there')
    {
        $controllers = $config->controllers['project'];
        $views = $config->views;
        return view('sketchpad::example', compact('name', 'controllers', 'views'));
    }

    // your methods here...

}
